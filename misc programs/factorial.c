#include <stdio.h>

int main()
{ int n;

int f[n];
int count=0;
int j=n;
scanf("%d",&n);
for (int i=0;i<n;i++)
{scanf("%d",&f[n]);}
for (int i=1;i<=n;i++)
{while (j>0)
    {if(f[i]!=f[j])
        {printf("%d\n",f[i]);
            j--;
        }
        
        
    } }

    return 0;
}